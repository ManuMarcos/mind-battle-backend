server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/users/**
  config:
    import:
      - "shared-config.yml"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

